name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  gourmand:
    name: Code Quality (Gourmand)
    uses: crunchtools/gatehouse/.github/workflows/gourmand.yml@v0.5.0
